import React from 'react';
import styled from 'styled-components';
// import CourseInfoHeader
// import CourseInfoTags

const CourseInfo = ({ icon, title, tags, description, onClose }) => {
    return (
        <></>
    );
};

export default CourseInfo;